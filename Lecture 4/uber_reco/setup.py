from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='it will recomend prise to near by perosn as per distance',
    author='sagar',
    license='MIT',
)
